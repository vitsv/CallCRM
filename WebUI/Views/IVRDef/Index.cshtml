@model IEnumerable<Domain.Entities.AsteriskIVRDefinition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Name
        </th>
        <th>
            Extention
        </th>
        <th>
            CreationDate
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Extention)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreationDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AsteriskIVRDefinitionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AsteriskIVRDefinitionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AsteriskIVRDefinitionID })
        </td>
    </tr>
}

</table>
